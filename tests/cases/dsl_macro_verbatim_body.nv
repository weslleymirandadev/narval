# Expected:
# query( SELECT 1 )
# query( SELECT * FROM users )

comptime def sql!(src: str): str {
    return "query(" + src + ")";
}
write(sql! { SELECT 1 });
q = sql! { SELECT * FROM users };
write(q);
