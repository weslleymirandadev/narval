# Expected:
# Error: derive_bad must return the members as a str

comptime def derive_bad(cls: str, fields: array): int {
    return 1;
}
@[derive(bad)]
class B {
    x: int;
}
write("nunca compila");
