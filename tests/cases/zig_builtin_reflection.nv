# Expected:
# User
# true
# age
# name

class User {
    public mut name: str;
    public mut age: int;
}
write(@typeName(User));
write(@hasField(User, "age"));
comptime for f in @fieldNames(User) {
    write(f);
}
