# Expected:
# 1.000000
# 2.540302
# File: mymath2.h
# double sin(double x);
# double cos(double x);
# double exp(double x);
# double log(double x);
# double sqrt(double x);

comptime import_c("mymath2.h", link: "m");

@[diff(x, "dloss_dx")]
def loss(x: float): float {
    return x * x + sin(x);
}

write(dloss_dx(0.0));
write(dloss_dx(1.0));
